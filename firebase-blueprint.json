{
  "entities": {
    "committee": {
      "title": "Committee",
      "description": "A Model United Nations committee session containing its name, description, creator, status and creation timestamps",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier of the committee"
        },
        "name": {
          "type": "string",
          "description": "Name of the committee, e.g. UNSC"
        },
        "description": {
          "type": "string",
          "description": "Main topic or description of the committee"
        },
        "creatorId": {
          "type": "string",
          "description": "The Firebase Auth UID of the administrator who created the committee"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the committee was created"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the committee was last updated"
        }
      },
      "required": ["id", "name", "creatorId", "createdAt", "updatedAt"]
    }
  },
  "firestore": {
    "/committees/{committeeId}": {
      "schema": "committee",
      "description": "Collection of MUN committees managed in the Gavel platform"
    }
  }
}
